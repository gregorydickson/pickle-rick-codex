# Compiled TypeScript runtime output (regenerated by `npx tsc` / `npm run build` / install.sh).
# rootDir:"src" + outDir:"." strips the src/ prefix, emitting siblings here.
# Shell assets in bin/ are generated from src/scripts/ by the build (single source of truth).
/bin/
/services/
/types/
*.tsbuildinfo
